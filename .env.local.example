# Jina AI embeddings API key.
# Get a free one (1M tokens) at: https://jina.ai/?sui=apikey
#
# Used by:
#   - scripts/build-embeddings.mjs  (build time, calls `jina embed` CLI)
#   - app/api/search/route.ts       (runtime, called by the top-bar search box)
JINA_API_KEY=
